# Mission 0: Get the code, the professional way

**Name: Brianna Edwards**
**GitHub username: dinoduckk**

## Evidence

### `git remote -v`
```
answers\img\remote-v.png
```

### `git branch`
```
answers\img\branch.png
```

### `git status` before the `.gitignore` fix
```
answers\img\gitstatusbefore.png
```

### `git status` after the `.gitignore` fix
```
answers\img\gitstatusafter.png
```

## Questions

1. Which folder should not be committed, and why? Give one practical reason and one security-related reason.

   > I would think attacks would be the folder that you would not want public because it reveals what is vulnerable in your code.

2. What line or lines did you add to `.gitignore`? What does a trailing `/` mean in a `.gitignore` pattern?

   > I just added "/attacks/" which is the name of the folder to ignore. I added /'s because that tells the file that it is a folder, not a file.

3. **Connections:** in one or two sentences, what is the difference between a **fork** and a **clone**? Which one lives on GitHub and which one lives on your machine?

   > A fork is duplicating a repo, while cloning is still connected.

## Documentation log

| Page I used, with URL | One thing I learned from it |
|---|---|
| | |
